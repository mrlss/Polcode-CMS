{
  "collectionName": "components_shared_themes",
  "info": {
    "displayName": "Theme",
    "description": "Section background color + text contrast"
  },
  "options": {},
  "attributes": {
    "background": {
      "type": "enumeration",
      "default": "white",
      "enum": [
        "white",
        "black",
        "cream",
        "purple"
      ]
    },
    "textColor": {
      "type": "enumeration",
      "default": "dark",
      "enum": [
        "light",
        "dark"
      ]
    }
  }
}
